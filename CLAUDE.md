# CASTLOG — CLAUDE.md

## 1. 프로젝트 정체성
캐스트로그(Castlog) — 창업교육/창업컨설팅 기업 대상 멀티테넌트 B2B SaaS.
도메인: castlog.kr
프로젝트 생성 기반 작업 프로세스 관리 + 전자결재. 화이트라벨 지원(Phase 2).
플랫폼 이용료 과금 로직 없음(사용량 계측은 함).

## 1-1. 라우팅 규칙 (Phase 1 확정)
- 테넌트 경로: castlog.kr/{tenant-slug}/...  (서브도메인은 Phase 2 이후)
- 전문가 포털: castlog.kr/expert — 테넌트에 종속되지 않는 전역 경로
- 공개 링크(인쇄물·문자 발송 대상)는 테넌트 경로 밖 짧은 전용 경로:
  Phase 1: /e 섭외동의 · /j 전문가등록 · /d 서류제출 · /u 수신거부(법적 필수)
  Phase 2+: /s 설문 · /r 모집신청 · /g 방명록서명 · /c 위촉장서명 · /i 행사안내
            /m 멘토링 · /f 자료다운로드 · /v 수료증확인 · /a 출석체크
- **모든 링크는 설정 기반 base URL로 생성.** 도메인·경로 하드코딩 금지.
  (인쇄된 QR과 발송된 문자는 회수 불가 — 경로가 바뀌어도 살아있어야 함)
- 슬러그는 영문 소문자 kebab-case만. 한글 금지(인코딩 깨짐).
- 슬러그 예약어: a~z 단일 문자 26개 전부 + expert, admin, platform-admin,
  api, login, logout, signup, static, assets, public, help, support, about
  (단일 문자를 전부 막아두면 새 공개 경로가 필요할 때 즉시 쓸 수 있다)
- URL 슬러그는 보안 경계가 아니다. 권한은 항상 JWT의 tenant_id로 판정하고,
  URL 슬러그와 세션 테넌트가 불일치하면 리다이렉트 또는 거부.

## 1-2. 기능 모듈 구조 (테넌트별 선택 사용 — 확정)
플랫폼 기능은 "공통 기반 + 선택 모듈 3축"으로 나뉜다. 각 테넌트는 모듈을
하나만 쓰거나 조합해 쓸 수 있다. 플랫폼 전 기능 강제 제공 없음.
- 공통 기반(항상 활성): 계정·권한, 테넌트 설정, 발송 인프라, 감사로그, 통계 기반
- experts    전문가 섭외·관리 — 풀·등록·연결, 서류, 섭외, 지급·세무
- approvals  품의·전자결재 — 품의서, 결재라인·전결규정, 대결·위임, 병렬 합의
- operations 프로젝트·행사 운영 — 21스텝 라이프사이클, 일정, 공개링크(설문·모집 등)
- insights(통계·AI 보고서)는 향후 분리 예약 키 — 지금은 공통 기반에 포함
규칙:
1. 활성 여부는 tenants.feature_flags.modules 에 저장 (lib/modules). 미설정 = 전부 활성.
2. 신규 기능은 소속 축(공통/모듈)을 먼저 정한다 (작업 전 질문 목록에 포함).
3. 모듈 게이트는 서버에서 강제(requireModule / 서버 액션 검사).
   네비게이션 숨김만으로 처리 금지 — URL 직접 접근이 가능하다.
4. 모듈 간 연동 기능(예: 지급 품의)은 양쪽 모듈이 모두 활성일 때만 노출.
   한쪽만 활성이면 단독 동작 경로를 제공한다 (예: 결재 없이 지급 단순 확정).
5. experts는 operations 없이 단독 동작해야 한다 (풀 관리·서류 수집은 프로젝트 비의존).

## 2. 기술 스택 (변경 금지)
- Next.js 14 App Router + TypeScript (strict)
- Tailwind CSS + shadcn/ui
- Supabase (서울 리전) — DB·인증·민감서류 Storage
- 대용량 원본: 저비용 오브젝트 스토리지 또는 외부 링크
- sharp (이미지 리사이즈), SheetJS (엑셀), react-signature-canvas (서명)
- SMS: 알리고/NHN Cloud · 이메일: Resend · 지도: 네이버
- Anthropic API (보고서 보완·제안서 검수, 서버 전용)
- 결제 없음 / i18n 없음(한국어 전용)

## 3. 멀티테넌시 원칙 (최우선)
- tenant_id는 **JWT app_metadata에서만** 읽는다.
  요청 파라미터·user_metadata에서 읽는 코드는 즉시 보안 결함으로 간주.
- 테넌트 데이터 테이블은 전부 tenant_id + RLS. 신규 테이블은 RLS부터 작성.
- 전문가가 여러 기업에 연결된 경우, 세션의 '활성 테넌트'를 서버에서 검증 후 설정.

## 4. 전문가 신원 모델 (v1.2 핵심)
- experts / expert_documents 는 **전역 테이블** (전문가가 소유)
- 기업은 expert_tenant_links 가 있는 전문가만 조회 가능
- 서류 열람은 expert_document_grants 허용 범위 내에서만
- 섭외이력·의뢰비용·평판점수·프로젝트 정보는 **테넌트 격리** — 절대 교차 노출 금지
- 전문가 본인은 자신의 전 기업 통합 이력을 볼 수 있음

## 5. 민감정보 처리
- 주민등록번호(계약시점 키위임 + 10계층 방어. 상세는 설계문서 4.4·8.7절):
  * **기업은 번호를 보유하지 않는다.** 필요 시점에 조회만 한다.
  * **동의가 근거가 아니다.** 근거는 소득세법(원천징수·지급명세서).
    화면 문구는 "동의합니다"가 아니라 "확인합니다"(고지 확인).
  * 권한 발생 시점: 전문가의 섭외 승인 = 계약 성립.
    이때 해당 프로젝트·해당 기업 권한자용으로만 키가 래핑된다(tax_project_grants).
    지급 완료 + 법정 보존기간 경과 시 래핑 자동 폐기.
  * **항목별로 정책이 다르다. 하나로 묶지 말 것:**
    이력서 = 계약기간 내 자유 열람(로그만) / 통장사본 = 지급 단계 열람(전건 로그)
    주민번호 = 프로젝트당 2회 한도 + 재인증 + 전문가 즉시 알림
  * 2회 초과는 **차단하지 않는다.** 사유 기재 + 대표 승인 + 전문가 통지로 처리
    (세무조사·경정청구 등 정당한 초과 사유가 실제로 발생함)
  * **기본 경로는 화면 조회가 아니라 지급명세서 파일 자동생성.**
    화면 단건 조회는 예외 처리용. 파일 생성도 조회로 간주해 로그·통지.
  * 사업자 청구 전문가는 수집 금지. 지급 확정 시점에만 수집. 보존기간 후 자동 파기.
  * 저장: 앞/뒤 분할하여 별도 저장소(별도 자격증명·별도 키)에 보관.
  * 암호화: 레코드 DEK → 테넌트 키 → **조회 비밀번호에서 유도한 키(Argon2id)** 로 래핑.
    조회 비밀번호는 어디에도 저장하지 않는다. 에스크로·마스터 복호화 키를 만들지 않는다.
  * 복호화는 **분리된 전담 서비스**에서만. 메인 앱에 복호화 능력을 두지 않는다.
  * **일괄 조회 API를 만들지 않는다** (1요청 = 1건). 사용자·테넌트별 시간당 복호화 상한 강제,
    초과 시 자동 잠금 + 경보.
  * 승인된 지급 결재건에 연결되지 않은 조회 요청은 거부. 상시 조회 화면 없음.
  * 조회 권한: tax_access_grants 지정자(회계담당자·대표자)만.
    **플랫폼관리자(넥스트랩)도 조회 불가.** 대결·위임 대상에서 제외(코드로 강제).
  * 평문을 DB·로그·캐시·APM·클라이언트 상태에 저장 금지. 마스킹 필터를 로그 파이프라인 최전단에.
  * 화면 노출 시 조회자·시각 워터마크. 일정 시간 후 자동 재마스킹.
  * 모든 조회는 tax_access_logs 기록 + 전문가 본인에게 즉시 알림 및 이력 공개.
  * 허니토큰(tax_honeytokens) 복호화 시도 감지 시 즉시 전체 잠금.
- 신분증사본·통장사본·서명: 암호화 버킷, 서명된 만료 URL로만 열람, 공개 URL 금지.
- 민감서류는 리사이즈하지 않는다(원본 판독 필요).
- consulting_report_ai_augmented는 role='expert' 세션에서 SELECT 차단.
  API 응답에도 해당 리소스를 참조하는 값을 절대 포함하지 않는다.

## 5-1. 발송 유형 분리 (법적 필수)
- 발송 시 유형 선택 필수. 기본값은 광고성(안전한 쪽).
  업무연락 = 섭외요청·서류요청·일정·지급 안내 (사전동의 불요)
  광고성 = 신규기회 홍보·플랫폼 소식 (사전동의 필수)
- 광고성 선택 시 시스템이 강제: 미동의자 자동 제외, (광고) 표기 자동 삽입,
  수신거부 링크(/u) 자동 삽입(제거 불가), 야간 발송 차단
- 광고성 수신동의(ad_consents)는 개인정보 수집·이용 동의와 **별도 테이블**로 관리
- 이유: 시스템 제공자도 위법 발송의 책임 주체가 될 수 있다. 약관으로 면책 안 됨

## 5-2. SMS 공급자 구조 (확정 — 플랫폼 일괄 공급 안 함)
- 업무·광고 SMS는 **테넌트별 BYO 공급자**: 각 테넌트가 공급자(솔라피/알리고/NHN 등)를
  선택하고 자사 API 키·자사 발신번호(사전등록)를 등록해 발송한다.
  구현은 공급자 어댑터 인터페이스 + tenant_sms_configs(API 키 암호화 저장) — 단계 14.
- 넥스트랩도 테넌트로서 발송할 때는 다른 테넌트와 동일하게 자사 설정(솔라피)을 따른다.
- **예외 — 인증 OTP**(전문가 로그인·등록 인증번호)는 테넌트 귀속이 불가능한 전역 발송:
  Supabase Send SMS Hook → 플랫폼 운영사(넥스트랩) 솔라피 계정으로 발송. 사용량 계측.
- Supabase 내장 SMS 공급자(Twilio 등)는 사용하지 않는다.

## 6. 파일·스토리지 규칙
- 이미지 업로드는 반드시 리사이즈 파이프라인 경유(프리뷰 2000px / 썸네일 400px).
- 사진은 긴 변 1600px로 리사이즈 후 저장.
- 대용량 디자인 원본은 기본적으로 플랫폼에 저장하지 않는다.
  file_assets에 원본 저장방식(외부링크/아카이브/미보관)을 명시.
- 단일 파일 용량 상한·허용 확장자를 서버에서 검증.
- 테넌트별 스토리지 사용량을 tenant_usage_metrics에 기록.

## 7. 전자결재 엔진 규칙
- approval_rules: 금액 구간·결재유형·프로젝트유형 조건 → 결재라인 자동 결정(전결규정)
- approval_delegations: 대결·위임(기간·범위). 대결 처리 건은 원 결재자와 대결자 병기
- 병렬 합의 지원(순서 없는 동시 승인)
- 반려 후 재상신은 새 결재건 + 원 결재건 연결(이력 추적)
- 모든 결재 행위는 audit_logs 기록(INSERT 전용)

## 8. 워크플로우 규칙
- project_lifecycle_steps는 오케스트레이션 전용(스텝유형·순서·상태·담당자·기한)
- 업무 데이터를 JSON 블롭으로 저장 금지 — 반드시 전용 정규화 테이블
- 프로젝트 복제 시 구성 정보만 복제, 실적 데이터는 제외

## 9. TypeScript / React 규칙
- strict, any 금지(불가피시 unknown + 타입가드)
- Server Component 우선, 'use client'는 인터랙션 필요시만
- 데이터 페칭은 Server Component에서 직접 (useEffect+fetch 금지)
- 폼은 React Hook Form + Zod

## 10. 반응형 정책 (역할별 차등)
- 전문가 전 기능·결재 승인/반려·프로젝트 조회: 모바일 완전 대응(최우선)
- 프로젝트 설정 21스텝·백오피스 입력: 조회 가능 수준, 입력은 PC 최적화

## 11. 절대 금지 (Hard NO)
1. tenant_id를 JWT 외의 경로에서 읽기
2. 컨설턴트 role에 AI 보완 보고서 노출(직접/간접)
3. 주민등록번호 평문 저장·로그 기록·클라이언트 상태 보관, 권한자 외 조회 경로 제공
4. 주민번호 일괄 조회 API 생성, 에스크로·마스터 복호화 키 생성, 메인 앱에 복호화 능력 부여
5. project_lifecycle_steps에 업무 데이터 JSON 저장
6. 리사이즈 파이프라인 없이 이미지 원본 직접 저장
7. localStorage·sessionStorage 사용
8. Supabase 미국·유럽 리전
9. service_role 키 클라이언트 노출
10. raw SQL 직접 작성 / any 타입 / useEffect+fetch
11. 결제·PG·i18n 관련 코드 추가(요구사항에 없음)

## 12. 항상 해야 할 것 (Always)
1. 새 테이블 → tenant_id + RLS (전역 테이블이면 별도 접근 정책 명시)
2. 새 폼 → Zod 스키마
3. 새 발송 → sms_logs/email_logs 기록
4. 민감정보 조회 → audit_logs 기록
5. 파일 업로드 → 용량·확장자 서버 검증 + 리사이즈
6. 새 목록 화면 → 엑셀 내보내기 고려
7. 새 통계 → 사업연도 축 포함
8. 결재 관련 변경 → 조건부 라우팅·대결 경로 영향 확인

## 13. 작업 시작 전 항상 묻기
1. 설계 문서 몇 장에 해당하나?
2. 어느 역할 대상인가?
3. 어느 축 소속인가 — 공통 기반 / experts / approvals / operations (1-2번 참조)?
   모듈 소속이면 서버 게이트(requireModule)를 적용했는가?
4. 전역 데이터인가 테넌트 격리 데이터인가?
5. 민감정보를 다루는가? 암호화·로그·파기가 필요한가?
6. 새 테이블/컬럼이 필요한가? 마이그레이션 작성했는가?
7. 결재가 필요한 행위인가?
8. 모바일 대응 수준은 어디까지인가(10번 참조)?
