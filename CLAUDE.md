# CASTLOG — CLAUDE.md

## 1. 프로젝트 정체성
캐스트로그(Castlog) — 창업교육/창업컨설팅 기업 대상 멀티테넌트 B2B SaaS.
도메인: castlog.kr
프로젝트 생성 기반 작업 프로세스 관리 + 전자결재. 화이트라벨 지원(Phase 2).
플랫폼 이용료 과금 로직 없음(사용량 계측은 함).

## 1-1. 라우팅 규칙 (Phase 1 확정)
- 테넌트 경로: castlog.kr/{tenant-slug}/...
  * 슬러그 루트 한 칸(castlog.kr/{slug})은 **공개 진입 화면**이다 — 회사 로고·이름과
    들어가는 문 세 개만 보인다. 그 아래는 전부 로그인 필수.
  * **서브도메인({slug}.castlog.kr)은 채택하지 않는다** (판정: docs/decisions/tenant-address.md).
    Supabase 세션 쿠키를 `.castlog.kr`로 넓히면 모든 서브도메인이 세션을 공유하고,
    호스트별로 두면 테넌트 이동·관리모드 전환마다 재로그인이 된다. 주민등록번호를
    다루는 제품에서 주소 편의를 위해 살 위험이 아니다.
  * 기업 자체 도메인(hr.그회사.co.kr)은 요구가 실제로 나온 테넌트에 한해 연다
    (tenant_domains 매핑 + 소유 확인 + 인증서 감시). 경로 방식은 어느 경우에도 지우지 않는다.
- 전문가 포털: castlog.kr/expert — 테넌트에 종속되지 않는 전역 경로
- 공개 링크(인쇄물·문자 발송 대상)는 테넌트 경로 밖 짧은 전용 경로:
  Phase 1: /e 섭외동의 · /j 전문가등록 · /d 서류제출 · /u 수신거부(법적 필수)
  Phase 2+: /s 설문 · /r 모집신청 · /g 방명록서명 · /c 위촉장서명 · /i 행사안내
            /m 멘토링 · /f 자료다운로드 · /v 수료증확인 · /a 출석체크
- **모든 링크는 설정 기반 base URL로 생성.** 도메인·경로 하드코딩 금지.
  (인쇄된 QR과 발송된 문자는 회수 불가 — 경로가 바뀌어도 살아있어야 함)
- 슬러그는 영문 소문자 kebab-case만. 한글 금지(인코딩 깨짐).
- 슬러그 예약어: a~z 단일 문자 26개 전부 + expert, admin, platform-admin,
  api, login, logout, signup, static, assets, public, help, support, about
  (단일 문자를 전부 막아두면 새 공개 경로가 필요할 때 즉시 쓸 수 있다)
- URL 슬러그는 보안 경계가 아니다. 권한은 항상 JWT의 tenant_id로 판정하고,
  URL 슬러그와 세션 테넌트가 불일치하면 리다이렉트 또는 거부.

## 1-2. 기능 모듈 구조 (테넌트별 선택 사용 — 확정)
플랫폼 기능은 "공통 기반 + 선택 모듈 3축"으로 나뉜다. 각 테넌트는 모듈을
하나만 쓰거나 조합해 쓸 수 있다. 플랫폼 전 기능 강제 제공 없음.
- **공통 기반(항상 활성)** — 어떤 모듈 조합을 쓰든 일은 프로젝트를 여는 데서
  시작한다. 아래는 모듈 게이트를 걸지 않는다:
  * 프로젝트 개설·기본정보 (행사명·발주처·사업연도·기간·예산·설명)
  * PM(1명)·부PM(제한 없음)·담당자 배정
  * 세션별 정보 (날짜·시간·세션명·역할·필요인원·1인당 비용·장소)
  * **전문가 코드넘버(넘버링코드) 자동 발급** — 코드넘버까지가 공통이고,
    그 자리에 실제 전문가를 붙이는 것부터 experts다
  * 프로젝트 목록·대시보드·임원 현황·엑셀 내보내기, 종료 기여도 배분
  * 계정·권한(6단계)·위임, 조직·직급, 테넌트 설정
  * 발송 인프라(문자·이메일·수신거부), 감사로그, 사용량 계측, 알림함, 통계 기반
- experts    전문가 섭외·관리 — 풀·등록·연결, 서류, 섭외요청·수락서, 평가·등급,
             세션 안내문자, 지급·세무
- approvals  품의·전자결재 — 품의서, 결재라인·전결규정, 대결·위임, 병렬 합의
- operations 행사 운영 심화 — 21스텝 라이프사이클, 공개링크(설문·모집 등),
             프로젝트 복제, 보고서
- insights(통계·AI 보고서)는 향후 분리 예약 키 — 지금은 공통 기반에 포함
규칙:
1. 활성 여부는 tenants.feature_flags.modules 에 저장 (lib/modules). 미설정 = 전부 활성.
2. 신규 기능은 소속 축(공통/모듈)을 먼저 정한다 (작업 전 질문 목록에 포함).
3. 모듈 게이트는 서버에서 강제(requireModule / 서버 액션 검사).
   네비게이션 숨김만으로 처리 금지 — URL 직접 접근이 가능하다.
4. 모듈 간 연동 기능(예: 지급 품의)은 양쪽 모듈이 모두 활성일 때만 노출.
   한쪽만 활성이면 단독 동작 경로를 제공한다 (예: 결재 없이 지급 단순 확정).
5. experts는 operations 없이 단독 동작해야 한다 (풀 관리·서류 수집은 프로젝트 비의존).
6. **모듈 연결이 이 플랫폼의 핵심 기술이다 (기획 확정).** 세부 기능은 조정될 수 있으나
   3개 대표영역의 효과적 연결은 최우선 설계 대상이다. 핵심 연동 지점:
   - experts ↔ approvals: 지급 품의 (결재 승인 → 지급 확정), 섭외 품의(선택)
   - operations ↔ experts: 프로젝트 단위 전문가 섭외·배정·의뢰비용
   - operations ↔ approvals: 프로젝트 품의(예산·집행), 스텝별 결재 게이트
   연동 규칙: 연동 상태는 전용 연결 테이블로 표현(양쪽 도메인 테이블 직접 오염 금지),
   연동 로직은 lib/integrations/에 격리, 모듈 비활성 시 연결 테이블 접근도 차단.
7. 테넌트 생성·설정(단계 8)에서 모듈 조합 선택을 제공한다.
   조합은 계약(플랜) 정보일 뿐 결제 기능은 만들지 않는다 (11번 Hard NO 유지).
8. **모듈은 나중에 추가할 수 있고, 추가하면 기존 데이터에 이어져야 한다.**
   기업 대표가 설정에서 추가를 요청(tenant_module_requests) → 캐스트로그
   관리모드에서 승인하면 feature_flags에 반영된다. 모듈을 껐다 켜도 데이터는
   지워지지 않는다(게이트는 앱 레벨, RLS는 tenant_id 기준).
   후행 활성 시 기존 데이터를 잇는 경로를 반드시 제공한다:
   - operations 추가 → 프로젝트 미연결 섭외 건을 프로젝트에 붙일 수 있어야 한다
   - approvals 추가 → 대기 중인 지급 배치를 지급 품의로 상신할 수 있어야 한다
   - experts 추가 → 이미 만들어 둔 세션·코드넘버를 그대로 사용해야 한다
   활성 직후에는 무엇이 열렸는지·무엇을 이어야 하는지 온보딩 안내를 띄운다.

## 2. 기술 스택 (변경 금지)
- Next.js 14 App Router + TypeScript (strict)
- Tailwind CSS + shadcn/ui
- Supabase (서울 리전) — DB·인증·민감서류 Storage
- 대용량 원본: 저비용 오브젝트 스토리지 또는 외부 링크
- sharp (이미지 리사이즈), SheetJS (엑셀), react-signature-canvas (서명)
- SMS: 알리고/NHN Cloud · 이메일: Resend · 지도: 네이버
- Anthropic API (보고서 보완·제안서 검수, 서버 전용)
- 결제 없음 / i18n 없음(한국어 전용)

## 3. 멀티테넌시 원칙 (최우선)
- tenant_id는 **JWT app_metadata에서만** 읽는다.
  요청 파라미터·user_metadata에서 읽는 코드는 즉시 보안 결함으로 간주.
- 테넌트 데이터 테이블은 전부 tenant_id + RLS. 신규 테이블은 RLS부터 작성.
- 전문가가 여러 기업에 연결된 경우, 세션의 '활성 테넌트'를 서버에서 검증 후 설정.

## 3-1. 기업 내 권한 체계 (6단계 — 기획 확정)
- 권한단계: ceo(대표) > director(이사) > team_lead(팀장) > deputy(대리)
  > senior(주임) > staff(사원). 저장은 users.grade, 판정은 JWT app_metadata.grade.
- **users.role(org_admin/manager/staff)은 grade에서 DB 트리거가 파생시키는 호환값이다.**
  코드에서 role을 직접 부여·변경하지 않는다 — 항상 grade를 바꾼다.
  매핑: ceo→org_admin, director·team_lead→manager, 그 외→staff (app.role_from_grade)
- 두 축을 분리한다:
  * 실행 권한(섭외요청·수락서·결재 상신 등) = role 기준 (팀장까지 manager)
  * 열람 범위(전사 프로젝트) = app.can_view_all_projects() — **대표·이사만 전체**,
    팀장 이하는 project_assignments에 배정된 프로젝트만. 배정은 권한자(대표·이사)가 한다.
- 대표는 'CEO 업무기능 + 시스템 설정·관리기능'을 모두 갖는다. 이 중 **설정·관리기능만**
  스코프 단위로 위임할 수 있다 (tenant_admin_grants).
  서버 게이트는 requireAdminScope(scope), DB는 app.has_admin_scope(scope).
  * 스코프 9종: settings(회사 정보·기본 설정) / modules(사용 기능 요청) /
    staff(임직원 계정·직급) / sending(문자 발송 자격증명) / templates(발송 문구) /
    approvals(전결규정) / audit(감사로그·보안 현황 열람) / backup(데이터 반출) /
    finance(지급·정산 금액).
  * **위험도가 갈리는 지점에서 자른다.** 자격증명 ↔ 문구, 읽기 ↔ 반출,
    회사 설정 ↔ 계약(모듈 요청)은 서로 다른 권한이다. 한 스위치에 묶으면
    필요한 권한을 주려다 필요 없는 권한까지 준다.
  * **넓은 스코프는 하위를 포함한다** (settings⊃modules, sending⊃templates,
    audit⊃backup). 앱은 lib/auth/admin-scope-keys.ts SCOPE_IMPLIES,
    DB는 app.has_admin_scope가 같은 규칙으로 판정한다.
  * sending 스코프는 SMS 자격증명(tenant_sms_configs: API 키·발신번호·활성 여부)까지
    포함한다. 키는 암호화 저장되고 화면에 재표시되지 않으며, 변경·테스트는 감사로그에 남는다.
  * backup 반출 범위는 그 사람의 RLS 열람 범위까지다(팀장 이하는 배정 프로젝트만).
    전사 반출이 필요하면 대표·이사에게 준다.
  * 위임 화면은 **기능이 행, 담당자가 열**이다. 기능마다 다른 사람을 둘 수 있어야
    한다 — 발송은 홍보, 지급은 회계, 계정은 인사가 실제 주인이다.
- **위임 금지 대상(코드로 강제):**
  1. 세무(주민등록번호) 조회 지정자 관리 — tax_access_grants는 계속 app.is_org_admin() 전용
  2. 위임 자체의 부여·회수 — 위임받은 사람은 재위임할 수 없다
  3. 대표(ceo) 등급 부여 — 대표만 가능

## 4. 전문가 신원 모델 (v1.2 핵심)
- experts / expert_documents 는 **전역 테이블** (전문가가 소유)
- 기업은 expert_tenant_links 가 있는 전문가만 조회 가능
- 서류 열람은 expert_document_grants 허용 범위 내에서만
- 섭외이력·의뢰비용·평판점수·프로젝트 정보는 **테넌트 격리** — 절대 교차 노출 금지
- 전문가 본인은 자신의 전 기업 통합 이력을 볼 수 있음

## 5. 민감정보 처리
- 주민등록번호(계약시점 키위임 + 10계층 방어. 상세는 설계문서 4.4·8.7절):
  * **기업은 번호를 보유하지 않는다.** 필요 시점에 조회만 한다.
  * **동의가 근거가 아니다.** 근거는 소득세법(원천징수·지급명세서).
    화면 문구는 "동의합니다"가 아니라 "확인합니다"(고지 확인).
  * 권한 발생 시점: 전문가의 섭외 승인 = 계약 성립.
    이때 해당 프로젝트·해당 기업 권한자용으로만 키가 래핑된다(tax_project_grants).
    지급 완료 + 법정 보존기간 경과 시 래핑 자동 폐기.
  * **항목별로 정책이 다르다. 하나로 묶지 말 것:**
    이력서 = 계약기간 내 자유 열람(로그만) / 통장사본 = 지급 단계 열람(전건 로그)
    주민번호 = 프로젝트당 2회 한도 + 재인증 + 전문가 즉시 알림
  * 2회 초과는 **차단하지 않는다.** 사유 기재 + 대표 승인 + 전문가 통지로 처리
    (세무조사·경정청구 등 정당한 초과 사유가 실제로 발생함)
  * **기본 경로는 화면 조회가 아니라 지급명세서 파일 자동생성.**
    화면 단건 조회는 예외 처리용. 파일 생성도 조회로 간주해 로그·통지.
  * 사업자 청구 전문가는 수집 금지. 지급 확정 시점에만 수집. 보존기간 후 자동 파기.
  * 저장: 앞/뒤 분할하여 별도 저장소(별도 자격증명·별도 키)에 보관.
  * 암호화: 레코드 DEK → 테넌트 키 → **조회 비밀번호에서 유도한 키(Argon2id)** 로 래핑.
    조회 비밀번호는 어디에도 저장하지 않는다. 에스크로·마스터 복호화 키를 만들지 않는다.
  * 복호화는 **분리된 전담 서비스**에서만. 메인 앱에 복호화 능력을 두지 않는다.
  * **일괄 조회 API를 만들지 않는다** (1요청 = 1건). 사용자·테넌트별 시간당 복호화 상한 강제,
    초과 시 자동 잠금 + 경보.
  * 승인된 지급 결재건에 연결되지 않은 조회 요청은 거부. 상시 조회 화면 없음.
  * 조회 권한: tax_access_grants 지정자만. **활성 최대 3명**(앱 + DB 트리거
    app.enforce_tax_grant_limit로 강제). 역할 표기는 회사 직급 체계(positions)에서
    고른다 — 회사마다 이 일을 맡는 자리 이름이 다르다.
    **플랫폼관리자(넥스트랩)도 조회 불가.** 대결·위임 대상에서 제외(코드로 강제).
  * 평문을 DB·로그·캐시·APM·클라이언트 상태에 저장 금지. 마스킹 필터를 로그 파이프라인 최전단에.
  * 화면 노출 시 조회자·시각 워터마크. 일정 시간 후 자동 재마스킹.
  * 모든 조회는 tax_access_logs 기록 + 전문가 본인에게 즉시 알림 및 이력 공개.
  * 허니토큰(tax_honeytokens) 복호화 시도 감지 시 즉시 전체 잠금.
- 신분증사본·통장사본·서명: 암호화 버킷, 서명된 만료 URL로만 열람, 공개 URL 금지.
- 민감서류는 리사이즈하지 않는다(원본 판독 필요).
- consulting_report_ai_augmented는 role='expert' 세션에서 SELECT 차단.
  API 응답에도 해당 리소스를 참조하는 값을 절대 포함하지 않는다.

## 5-1. 발송 유형 분리 (법적 필수)
- 발송 시 유형 선택 필수. 기본값은 광고성(안전한 쪽).
  업무연락 = 섭외요청·서류요청·일정·지급 안내 (사전동의 불요)
  광고성 = 신규기회 홍보·플랫폼 소식 (사전동의 필수)
- 광고성 선택 시 시스템이 강제: 미동의자 자동 제외, (광고) 표기 자동 삽입,
  수신거부 링크(/u) 자동 삽입(제거 불가), 야간 발송 차단
- 광고성 수신동의(ad_consents)는 개인정보 수집·이용 동의와 **별도 테이블**로 관리
- 이유: 시스템 제공자도 위법 발송의 책임 주체가 될 수 있다. 약관으로 면책 안 됨

## 5-2. SMS 공급자 구조 (개정 — 테넌트별 2방식 선택)
- 업무·광고 SMS는 테넌트가 둘 중 선택한다 (tenant_sms_configs.mode):
  * **byo** — 자사 공급자(솔라피/알리고/NHN 등) 계정·자사 API 키(암호화 저장)·자사 발신번호.
  * **platform** — 캐스트로그(넥스트랩) 솔라피 계정으로 발송. 자격증명은 서버
    환경변수(PLATFORM_SOLAPI_API_KEY/SECRET)에만 두고 DB에 저장하지 않는다.
    발신번호는 **그 기업의 번호**를 쓰되, 발신번호 사전등록제(전기통신사업법
    제84조의2) 때문에 캐스트로그 솔라피 계정에 회사마다 1회 등록(인증)이 필요하다
    — 법·통신사 규제라 코드로 우회 불가. 진입은 이용코드(PLATFORM_SMS_ACCESS_CODE)
    최초 1회 인증, 승인 시각만 저장(코드 변경이 기존 승인에 영향 없음).
  구현은 공급자 어댑터 인터페이스 + tenant_sms_configs — 단계 14 + 개정.
  * **platform(b)은 현재 보류** (사용자 지시). PLATFORM_SMS_ACCESS_CODE 미설정 시
    화면에 선택지가 나오지 않으며, env를 설정하면 코드 수정 없이 제공이 시작된다.
- 넥스트랩도 테넌트로서 발송할 때는 다른 테넌트와 동일하게 자사 설정(솔라피)을 따른다.
- **예외 — 인증 OTP**(전문가 로그인·등록 인증번호)는 테넌트 귀속이 불가능한 전역 발송:
  Supabase Send SMS Hook → 플랫폼 운영사(넥스트랩) 솔라피 계정으로 발송. 사용량 계측.
- Supabase 내장 SMS 공급자(Twilio 등)는 사용하지 않는다.

## 6. 파일·스토리지 규칙
- 이미지 업로드는 반드시 리사이즈 파이프라인 경유(프리뷰 2000px / 썸네일 400px).
- 사진은 긴 변 1600px로 리사이즈 후 저장.
- 대용량 디자인 원본은 기본적으로 플랫폼에 저장하지 않는다.
  file_assets에 원본 저장방식(외부링크/아카이브/미보관)을 명시.
- 단일 파일 용량 상한·허용 확장자를 서버에서 검증.
- 테넌트별 스토리지 사용량을 tenant_usage_metrics에 기록.

## 7. 전자결재 엔진 규칙
- approval_rules: 금액 구간·결재유형·프로젝트유형 조건 → 결재라인 자동 결정(전결규정)
- approval_delegations: 대결·위임(기간·범위). 대결 처리 건은 원 결재자와 대결자 병기
- 병렬 합의 지원(순서 없는 동시 승인)
- 반려 후 재상신은 새 결재건 + 원 결재건 연결(이력 추적)
- 모든 결재 행위는 audit_logs 기록(INSERT 전용)

## 8. 워크플로우 규칙
- project_lifecycle_steps는 오케스트레이션 전용(스텝유형·순서·상태·담당자·기한)
- 업무 데이터를 JSON 블롭으로 저장 금지 — 반드시 전용 정규화 테이블
- 프로젝트 복제 시 구성 정보만 복제, 실적 데이터는 제외

## 9. TypeScript / React 규칙
- strict, any 금지(불가피시 unknown + 타입가드)
- Server Component 우선, 'use client'는 인터랙션 필요시만
- 데이터 페칭은 Server Component에서 직접 (useEffect+fetch 금지)
- 폼은 React Hook Form + Zod

## 10. 반응형 정책 (역할별 차등)
- 전문가 전 기능·결재 승인/반려·프로젝트 조회: 모바일 완전 대응(최우선)
- 프로젝트 설정 21스텝·백오피스 입력: 조회 가능 수준, 입력은 PC 최적화

## 11. 절대 금지 (Hard NO)
1. tenant_id를 JWT 외의 경로에서 읽기
2. 컨설턴트 role에 AI 보완 보고서 노출(직접/간접)
3. 주민등록번호 평문 저장·로그 기록·클라이언트 상태 보관, 권한자 외 조회 경로 제공
4. 주민번호 일괄 조회 API 생성, 에스크로·마스터 복호화 키 생성, 메인 앱에 복호화 능력 부여
5. project_lifecycle_steps에 업무 데이터 JSON 저장
6. 리사이즈 파이프라인 없이 이미지 원본 직접 저장
7. localStorage·sessionStorage 사용
8. Supabase 미국·유럽 리전
9. service_role 키 클라이언트 노출
10. raw SQL 직접 작성 / any 타입 / useEffect+fetch
11. 결제·PG·i18n 관련 코드 추가(요구사항에 없음)

## 12. 항상 해야 할 것 (Always)
1. 새 테이블 → tenant_id + RLS (전역 테이블이면 별도 접근 정책 명시)
2. 새 폼 → Zod 스키마
3. 새 발송 → sms_logs/email_logs 기록
4. 민감정보 조회 → audit_logs 기록
5. 파일 업로드 → 용량·확장자 서버 검증 + 리사이즈
6. 새 목록 화면 → 엑셀 내보내기 고려
7. 새 통계 → 사업연도 축 포함
8. 결재 관련 변경 → 조건부 라우팅·대결 경로 영향 확인
9. **사용자에게 실패를 돌려줄 때 → 원인 분류(규칙 거부/상태 미충족/시스템 결함)와
   다음 행동을 문구에 포함. "실패했습니다"만 반환 금지.** RLS·권한 거부는
   lib/ux/action-errors.ts explainActionError 경유 — 규칙임을 명시하고 챗봇
   개선 요청 경로를 안내한다. 근거·CS 절차: docs/ops/cs-playbook.md

## 13. 작업 시작 전 항상 묻기
1. 설계 문서 몇 장에 해당하나?
2. 어느 역할 대상인가?
3. 어느 축 소속인가 — 공통 기반 / experts / approvals / operations (1-2번 참조)?
   모듈 소속이면 서버 게이트(requireModule)를 적용했는가?
4. 전역 데이터인가 테넌트 격리 데이터인가?
5. 민감정보를 다루는가? 암호화·로그·파기가 필요한가?
6. 새 테이블/컬럼이 필요한가? 마이그레이션 작성했는가?
7. 결재가 필요한 행위인가?
8. 모바일 대응 수준은 어디까지인가(10번 참조)?

## 14. 운영·품질 원칙 (APDF 가이드 채택분 — 판정 기록: docs/decisions/apdf-adoption.md)
1. **AI 경계**: AI(Anthropic API)는 설명·보완·문장화만 담당한다.
   권한·결재 승인·지급 확정·데이터 판정의 근거로 AI 응답을 사용하지 않는다.
   AI 설정(프롬프트 등)은 버전 관리·롤백 가능해야 한다.
2. **운영 설정화**: 자주 바뀌는 값(직급·결재라인·발송 템플릿·기능 플래그)은
   코드가 아니라 설정 테이블/관리자 화면으로 관리한다. 하드코딩 금지.
3. **위험 작업 보호**: 파괴적·되돌리기 어려운 작업(테넌트 중지, 계정 비활성화,
   데이터 파기, 발송 실행)은 2단계 확인 + 권한 분리를 적용한다.
4. **삭제보다 비활성화**: 삭제 대신 비활성화·상태 전환·복구 가능 상태를 우선한다.
   예외: 법정 보존기간 경과 민감정보의 자동 파기(5번 절)가 이 원칙보다 우선한다.
5. **발송·설정 변경**: 미리보기·예약·중지를 지원한다 (단계 14 요구사항).
   운영 설정(전결규정 등)은 개정 이력을 남긴다.
6. **디자인 잠금**: 브랜드 토큰·컴포넌트 계약을 준수한다. 확정 시안의 임의
   재해석 금지. 모바일을 데스크톱 축소판으로 구현 금지 (10번과 연계).
7. **더미 구현 금지**: 동작하지 않는 버튼·미연결 기능을 완료로 보고하지 않는다.
   단계 완료 보고에는 빌드·실동작 확인 결과를 포함한다.
8. **단계 승인 게이트**: 단계 완료를 보고하고 사용자 승인 후 다음 단계를
   시작한다. 승인되지 않은 범위를 임의로 구현하지 않는다.
9. **외부 가이드 유입 시**: 기존 확정 기획과의 충돌 여부를 판정하고
   docs/decisions/에 채택/제외 기록을 남긴 뒤 반영한다.
10. **릴리스 절차 (실고객 운영 중 — docs/ops/release-playbook.md):**
   일상 작업 PR은 develop으로, main은 릴리스(v1.N)와 핫픽스만.
   **마이그레이션은 코드 병합 전에 실행한다("SQL 먼저")** — 이를 위해
   마이그레이션은 추가 전용·멱등으로 작성하고, 파괴적 변경은 두 릴리스로
   쪼갠다(expand→contract). 신규 컬럼 참조 코드에는 부재 폴백을 둔다.
   사용 중 고객이 겪는 오류의 수정만 main 직행(핫픽스) 후 develop 역병합.

## 15. 작업 영역 구분 및 보류 목록 (기획 확정 — 잊지 말 것)
현재 구축 중인 영역은 **전문가 섭외(experts)** 다. 아래 항목은 각자의 영역이
열릴 때 다룬다. 지금 손대지 않는다.

- **21스텝 라이프사이클** → 전자결재(approvals) · 행사 운영 심화(operations)
  영역에서 구축한다. 스텝별 결재 게이트도 그때 함께 설계한다.
- **보고서 메뉴** → 행사 운영 심화(operations) 영역에서 구축한다.
  그때까지는 '준비 중' 화면을 유지한다.
- **네이버웍스 연동 (BYO 커넥터)** → 추후 작업. 보류 상태 유지.
- **SMS 캐스트로그 계정 발송(b안)** → 구현 완료 상태로 보류. 제공 시작은
  PLATFORM_SMS_ACCESS_CODE(+ PLATFORM_SOLAPI_API_KEY/SECRET) 설정만으로 된다.
- **AI 음성 업무보고/지시 (임원)** → 추후 작업. 보류 상태 유지.

## 16. 브랜드 정책 (화이트라벨 — 기획 확정)
캐스트로그는 **플랫폼**으로서 별도 마케팅을 한다. 그러나 실제 사용기업과
전문가가 만나는 모든 접점에서는 **그 기업의 이름과 로고가 중심**이다.
캐스트로그 브랜드는 그 자리에서 뒤로 물러난다.

- 회사 로고는 설정 > 내 설정에서 등록한다 (대표·'테넌트 설정' 위임자).
- 적용 범위: 기업 사용자 화면(사이드바·챗봇)은 물론, **전문가가 보는 모든
  화면**까지 — 공개 링크(/e /j /d /u), 수락서, 전문가 포털의 기업별 표시,
  발송 메일. 전문가는 '캐스트로그'가 아니라 '그 회사'와 일한다.
- 로고 미등록 테넌트는 캐스트로그 심볼로 폴백한다.
- 공개 화면용 로고는 `/api/tenant-logo/[tenantId]`로 노출한다. 로고는 명함·
  현수막에 쓰는 공개 브랜드 자산이므로 공개 경로를 허용한다 — 다만 그 경로는
  **로고만** 내보낸다(다른 테넌트 정보를 실어 보내지 않는다).
- 플랫폼 고지가 법적으로 필요한 자리(약관·개인정보·수신거부)에는 캐스트로그
  표기를 유지한다. 브랜딩과 법적 고지는 다른 문제다.
