/**
 * 전문가가 고를 수 있는 긴급 취소 사유.
 *
 * 자유 입력만 두면 대부분 비워 두거나 '개인 사정'만 적힌다. 기업은 대체 인력을
 * 구해야 하는 쪽이므로, 최소한 어떤 종류의 사유인지는 알아야 한다.
 *
 * 별도 모듈인 이유: "use server" 파일은 async 함수만 내보낼 수 있다. 상수를
 * 그 파일에서 내보내면 클라이언트 번들에서는 서버 액션 참조로 바뀌어
 * 배열이 아니게 되고, 섭외함 화면 전체가 `map is not a function`으로 죽는다
 * (렛츠 전문가 보고 2026-09-05 — 대시보드의 '섭외 요청 전체보기'·'지금
 * 확인하기'·'응답 필요'가 모두 이 화면으로 간다).
 */
export const EXPERT_CANCEL_REASONS = [
  "건강·질병",
  "일정 중복(다른 일정 확정)",
  "가족·경조사",
  "교통·이동 문제",
  "개인 사정",
  "기타",
] as const;

export type ExpertCancelReason = (typeof EXPERT_CANCEL_REASONS)[number];
